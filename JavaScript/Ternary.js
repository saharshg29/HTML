//Ternary operator

let points = 100;
// If a customer has more than 100 points 
// They are gold customers
// else They are Silver customer

points = 110
let type = points > 100 ? 'gold' : 'silver';
type
// statement followed by (?) are accessed only if the condiotion is set to true
// statement followed by (:) are accessed only if the condiotion is set to false