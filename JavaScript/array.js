// Array in javaScript
let selectedColor = ['red', 'blue'];
selectedColor
selectedColor[2] = 'green';
console.log(selectedColor);
// different type of data types can be stored into a javaScript array
// arrays in javaScript are dynamic
selectedColor.length
// Remember Array is a data structure used to store list of an item