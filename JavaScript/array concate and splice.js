// concat method of array is used to combine or join two differnet arrays
const first = [1, 2, 3, 4];
const second = [5, 6, 7, 8];

const combined = first.concat(second);
console.log(combined)

// slice method of array is used to substracat or remove a part of an array from the original array
console.log(combined.slice(2,5));

// Both slice and concat method returns an array