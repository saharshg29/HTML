// join is an another important method of array
// join method uses an optional parameter of string data type
// join method returns a string

const numbers = [1, 2, 3];
const joined = numbers.join('/');
console.log(joined);

// Slice method of string can also be used to perform the same action on strings

const message = 'This is a demo message. Why is it not working';
const parts = message.split('  ');
console.log(parts);